console.log(window);
console.log(location);
// console.log(location.reload());
// changing th url in javascript
// console.log(location.href = 'index.html')
// location.href = 'https://www.google.com'       like this we can change the url and go to that perticular url.
// understanding the history object in javascript
console.log(history);
history.back();
history.forward();
// navigate through pages using BOm in javascript
history.go(-3); 
history.go(4);
innerHeight
innerWidth
outerHeight
outerWidth
open()  //it will create the new window. it basically create the new window.
open('https://www.google.com')  // it will create the new window and search for the url.
close()  // by using the close we can close the window that open by the open() object

// resizing in javascript
// open('https://www.google.com' , 'google' , 'resizable')
resizeTo(600, 500);
moveTo(0 , 0);
scrollBy(0 , 500);
print();
document();


// DOM  => Document Object Model
function sayHii(){
    console.log('hii');
}
console.dir(sayHii);  // it basically shows in the object format
