const container = document.querySelector('.container');
const h1 = document.querySelector('h1');
const card = document.querySelector('.card');

// container.appendChild(h1);
// container.appendChild(h1.cloneNode(true))     by using this we can cope and append to the node
// container.appendChild('hello')    it will throw the error. we can't append string using appendChild.
// const newTextNode = document.createTextNode('Hello World');
// container.appendChild(newTextNode);


// container.append(h1);    
// container.append('hello');    by using the append we can append the sting 
// container.append('hello', h1 , 'world');